import Box from "@mui/material/Box";


interface ExamplePaper {
	children?: React.ReactNode;
	round?: 
}

export function ExamplePaper(props: ExamplePaper): JSX.Element {
	return (
		<Box sx={() => ({

		})}>
			{props.children}
		</Box>
	);
}
